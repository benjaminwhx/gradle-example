/** 这个文件主要是为了下载gradle的源码,以供查看 **/
apply plugin: 'java'

repositories {
    maven {
        url 'http://maven.aliyun.com/nexus/content/groups/public/'
    }
}

dependencies {
    compile group: 'org.gradle', name: 'gradle-core', version: '3.1'
}